[project]
name = "swgoh-packs"
version = "0.1.0"
description = "Telegram bot proxy for SWGOH"
dependencies = [
    "pyTelegramBotAPI==4.18.0",
    "requests==2.31.0",
    "Flask==3.0.2"
]

[tool.vercel]
entrypoint = "bot:app"
